services:
  frontend:
    build:
      context: ./client
    ports:
      - "5173:5173"
    depends_on:
      - backend

  backend:
    build:
      context: ./server
    ports:
      - "4000:4000"
    env_file:
      - ./server/.env